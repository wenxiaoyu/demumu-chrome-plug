# 快速开始指南

欢迎使用"还活着吗"Chrome 插件！本指南将帮助你快速上手开发。

## 📦 安装依赖

### 前置要求
- Node.js 18 或更高版本
- pnpm（推荐）或 npm

### 安装步骤

1. **克隆仓库**
```bash
git clone https://github.com/wenxiaoyu/alive-checker.git
cd alive-checker
```

2. **安装依赖**
```bash
pnpm install
```

3. **启动开发服务器**
```bash
pnpm dev
```

## 🔧 开发流程

### 1. 开发模式

启动开发服务器后，Vite 会自动构建插件到 `dist/` 目录，并支持热更新。

```bash
pnpm dev
```

### 2. 加载插件到 Chrome

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启右上角的"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择项目的 `dist` 文件夹

### 3. 查看插件

- 点击浏览器工具栏的插件图标，打开 Popup 页面
- 右键点击图标 → 选项，打开 Options 页面
- 在 `chrome://extensions/` 中点击"检查视图：Service Worker"，调试 Background

### 4. 开发调试

**调试 Popup 页面：**
- 右键点击插件图标 → 检查弹出内容

**调试 Options 页面：**
- 在 Options 页面右键 → 检查

**调试 Background：**
- 在 `chrome://extensions/` 中点击"检查视图：Service Worker"

**查看日志：**
- 所有日志都会带有 `[AliveChecker]` 前缀

## 🏗️ 项目结构

```
alive-checker/
├── openspec/              # 项目管理文档
│   ├── project.md         # 项目总览
│   ├── AGENTS.md          # 开发指引
│   └── changes/           # 各阶段规格
├── src/
│   ├── background/        # Background Service Worker
│   ├── popup/             # Popup 页面（React）
│   ├── options/           # Options 页面（React）
│   ├── content/           # Content Scripts
│   ├── shared/            # 共享代码
│   │   ├── types.ts       # 类型定义
│   │   ├── constants.ts   # 常量
│   │   └── storage.ts     # 存储管理
│   ├── icons/             # 图标
│   └── manifest.json      # 插件清单
└── dist/                  # 构建输出
```

## 📝 常用命令

```bash
# 开发
pnpm dev              # 启动开发服务器

# 构建
pnpm build            # 构建生产版本

# 代码质量
pnpm type-check       # TypeScript 类型检查
pnpm lint             # ESLint 代码检查
pnpm format           # Prettier 代码格式化

# 测试（计划中）
pnpm test             # 运行测试
```

## 🎯 开发阶段

### 当前阶段：阶段 0 - 项目搭建

**目标**：搭建项目基础架构

**任务清单**：查看 `openspec/changes/phase-0-setup/tasks.md`

### 下一阶段：阶段 1 - MVP 本地版

**目标**：实现核心签到和生命值功能

**规格文档**：查看 `openspec/changes/phase-1-mvp-local/spec.md`

## 📚 文档导航

- **项目总览**：`openspec/project.md`
- **开发指引**：`openspec/AGENTS.md`
- **项目状态**：`openspec/STATUS.md`
- **阶段 0 规格**：`openspec/changes/phase-0-setup/spec.md`
- **阶段 1 规格**：`openspec/changes/phase-1-mvp-local/spec.md`

## 🔍 常见问题

### Q: 修改代码后插件没有更新？

A: 开发模式下，Vite 会自动重新构建。但 Chrome 可能需要手动刷新：
1. 在 `chrome://extensions/` 中点击插件的刷新按钮
2. 或者重新加载插件

### Q: 如何查看 Background 的日志？

A: 在 `chrome://extensions/` 中找到你的插件，点击"检查视图：Service Worker"

### Q: TypeScript 报错怎么办？

A: 运行 `pnpm type-check` 查看详细错误信息

### Q: 如何添加新的依赖？

A: 使用 pnpm 安装：
```bash
pnpm add <package-name>          # 生产依赖
pnpm add -D <package-name>       # 开发依赖
```

### Q: 如何提交代码？

A: 项目使用 Husky 和 lint-staged，提交前会自动运行检查：
```bash
git add .
git commit -m "feat: add new feature"
```

提交信息格式：
- `feat:` 新功能
- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式
- `refactor:` 重构
- `test:` 测试
- `chore:` 构建/工具

## 🚀 下一步

1. 阅读 `openspec/AGENTS.md` 了解开发原则
2. 查看 `openspec/changes/phase-0-setup/tasks.md` 了解当前任务
3. 开始开发！

## 💬 获取帮助

- **GitHub Issues**：报告 bug 或提出功能建议
- **GitHub Discussions**：讨论和交流
- **文档**：查看 `openspec/` 目录下的详细文档

---

祝你开发愉快！🎉
